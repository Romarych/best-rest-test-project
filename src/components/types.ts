export type FormPropsType = {
    isOption: boolean
};

export type FormType = { 
    firstName: string, 
    password: string, 
    email: string, 
    secondName: string, 
    confirmPassword: string
};